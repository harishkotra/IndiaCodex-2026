name = "foundry/contracts"
version = "1.0.0"
description = "Smart contracts generated by Foundry — Autonomous Software Company on Cardano"
license = "Apache-2.0"
compiler = ">=1.1.0"

[requirements]
aiken-lang/stdlib = ">=2.1.0"

[contexts]
mainnet = { network = "mainnet" }
preprod = { network = "preprod" }

[contexts.preprod]
network = "preprod"
